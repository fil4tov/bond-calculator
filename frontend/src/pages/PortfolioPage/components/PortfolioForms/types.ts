export interface PurchaseFormValues {
  amountSpent: string;
  quantity: string;
  purchaseDate: string;
}

export interface SaleFormValues {
  amountReceived: string;
  quantity: string;
  saleDate: string;
}
